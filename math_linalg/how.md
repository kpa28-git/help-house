
# Cosine Similarity
(x * y) / (|x|*|y|)

# Matrix Multiplication (A B)
inner product of each row vector in A by each column vector in B
if A is m X n, and B is n X p - output will be m X p matrix

# Beta Coefficients of Multivariate Linear Regression (Y=Xw)
w = X'Y (X'X)^{-1}

